import type { FlowMeta } from '@/lib/types';

const meta: FlowMeta = {
  name: 'Checkout',
  description:
    'A three-step checkout demo: review the cart, confirm your info, and see the confirmation.',
};

export default meta;
