import type { FlowMeta } from '@/lib/types';

const meta: FlowMeta = {
  name: 'Dashboard 2026',
  description:
    'Dashboard 2026 refresh. Swipe left or tap the Marketplace chip to see the Marketplace details page — the header transitions from light to the blue gradient theme.',
};

export default meta;
