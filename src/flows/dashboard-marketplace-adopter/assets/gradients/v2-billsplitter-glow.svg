<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 375 679" preserveAspectRatio="none" fill="none">
  <defs>
    <filter id="bf1" x="-200%" y="-400%" width="500%" height="900%">
      <feGaussianBlur stdDeviation="65 42"/>
    </filter>
    <filter id="bf2" x="-200%" y="-400%" width="500%" height="900%">
      <feGaussianBlur stdDeviation="35 18"/>
    </filter>
    <filter id="bf3" x="-200%" y="-400%" width="500%" height="900%">
      <feGaussianBlur stdDeviation="32 22"/>
    </filter>
  </defs>
  <!-- Transparent base; ellipses only. Stacked on top of v2-billsplitter-base.
       Centers sit right at the balance-card top edge (viewport Y≈277 maps
       to SVG y≈266) so the bright cyan peak crests over the card top with
       the halo radiating UP into the hero area. -->
  <ellipse cx="191.95" cy="299" rx="148" ry="52" fill="#01F3D8" filter="url(#bf1)"/>
  <ellipse cx="194.995" cy="304" rx="135" ry="26" fill="#FFDB98" filter="url(#bf2)"/>
  <ellipse cx="189.469" cy="294" rx="96" ry="33" fill="#01F3D8" filter="url(#bf3)"/>
</svg>
